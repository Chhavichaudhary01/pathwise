import { test, expect } from "@playwright/test"; test("Full flow: register to dashboard to feedback", async ({ page }) => { await page.goto("/"); await expect(page.getByText(/Your AI-Powered Career/i)).toBeVisible(); }); test("Edge cases", async ({ page }) => { await page.goto("/"); await page.getByRole("button", { name: /Try Instant Demo/i }).click(); await expect(page.url()).toContain("/dashboard"); });
